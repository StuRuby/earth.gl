const STATE = {
    INITED: 'inited',
    ADDED: 'added',
    REMOVED: 'removed'
};

export const LAYER_STATE = {
    ...STATE,
    CLEARED: 'cleared'
};

export const OVERLAY_STATE = {
    ...STATE
};

export const EFFECT_STATE = {
    ...STATE
};